# ============================================================
# SatQuery — Person 2 VLM Dependencies
# ============================================================

# ----- Core ML -----
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.36.0
accelerate>=0.25.0
tokenizers>=0.15.0
sentencepiece>=0.1.99
tiktoken>=0.5.2

# ----- LoRA / PEFT -----
peft>=0.7.0
bitsandbytes>=0.41.0

# ----- Vision -----
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# ----- Data Processing -----
pandas>=2.1.0
pyarrow>=14.0.0
datasets>=2.16.0
tqdm>=4.66.0

# ----- Evaluation Metrics -----
nltk>=3.8.0
rouge-score>=0.1.2
pycocoevalcap>=1.2

# ----- Configuration -----
pyyaml>=6.0.1
python-dotenv>=1.0.0

# ----- API (for tool serving) -----
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0

# ----- Testing -----
pytest>=7.4.0
pytest-asyncio>=0.23.0

# ----- Utilities -----
huggingface-hub>=0.20.0
safetensors>=0.4.0
einops>=0.7.0
scipy>=1.11.0
scikit-learn>=1.3.0

# ----- Optional: Flash Attention (install separately on Linux) -----
# pip install flash-attn --no-build-isolation

# ----- Optional: GGUF support -----
# pip install llama-cpp-python
